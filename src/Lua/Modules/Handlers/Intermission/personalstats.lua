local module = {}

module.name = "Pers. Stats"

function module.think(p) // runs when selected
end

function module.draw(v,p)
	v.drawString(160, 100-4, "WORK IN PROGRESS!", V_YELLOWMAP, "center")
end

return module